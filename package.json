{
  "name": "ssr-lit-html",
  "version": "0.0.6",
  "main": "ssr-lit-html.js",
  "license": "MIT",
  "files": [
    "/ssr-lit-html.js",
    "/ssr-lit-html.js.map",
    "/ssr-lit-html.d.ts",
    "/lib/",
    "/src/",
    "!/src/test/"
  ],
  "scripts": {
    "build": "tsc",
    "gen-docs": "typedoc --readme none --mode modules --excludeNotExported --excludePrivate --exclude **/*_test.ts --out ./docs/api ./src",
    "test": "npm run build && wct --npm && npm run lint",
    "quicktest": "wct -l chrome -p --npm",
    "checksize": "uglifyjs ssr-lit-html.js -mc --toplevel | gzip -9 | wc -c",
    "format": "find src test | grep '\\.js$\\|\\.ts$' | xargs clang-format --style=file -i",
    "lint": "tslint --fix --project ./",
    "prepack": "npm run build",
    "nts": "ts-node --project tsconfig.json --ignore=false"
  },
  "dependencies": {
    "@types/parse5": "^5.0.0",
    "circular-json-es6": "^2.0.2",
    "nlit-html": "^0.9.0",
    "parse5": "^5.1.0"
  },
  "devDependencies": {
    "@types/node": "^10.12.11",
    "ts-node": "^7.0.1",
    "tslint": "^5.11.0",
    "typescript": "^3.2.1"
  },
  "typings": "ssr-lit-html.d.ts",
  "repository": "https://github.com/Liquid-JS/ssr-lit-html.git",
  "author": "Marko Novak <development@hitko.si>"
}
